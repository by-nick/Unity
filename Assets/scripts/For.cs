using UnityEngine;

public class For : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        for (int x = 0; x <= 10; x++)
        {
            Debug.Log("Oi Nick, você consegue!");
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
